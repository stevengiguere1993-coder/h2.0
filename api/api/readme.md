# 🧩 MonAgent Monday Proxy

Ce projet sert de **proxy léger hébergé sur Vercel** pour relayer les appels HTTP entre ChatGPT (ou un orchestrateur externe) et l’API principale hébergée sur Render (`https://mondayagent-bridge.onrender.com`).

## ⚙️ Objectif

Contourner les restrictions réseau qui empêchent ChatGPT d’appeler directement Render, en passant par un proxy autorisé sur Vercel.

## 🧱 Structure

